repositories {
	jcenter()
}

dependencies {

	compile 'com.netflix.nebula:gradle-info-plugin:3.2.1'

	testCompile('com.netflix.nebula:nebula-test:4.2.2') {
        exclude group: 'org.codehaus.groovy'
  }
}
